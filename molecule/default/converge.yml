---
- name: Converge
  hosts: all
  vars:
    git_server: "github.com"
    git_user: "LPARS"
    git_repo: "puppet"
    r10k_ssh_private_key: "{{ lookup('ansible.builtin.env', 'SSH_PRIVATE_KEY') }}"
  roles:
    - role: lpars.puppet_server
